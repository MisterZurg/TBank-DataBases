# Инструкция по выполнению домашнего задания

## 📘 Общая информация

Каждое задание — это отдельный SQL-запрос, который необходимо разместить в соответствующем файле в папке:

```
student_solutions/
```

Название файла совпадает с alias задания, например:

```
001_all_books.sql
002_unique_last_names.sql
003_authors_list.sql
...
```

Изменять можно **только содержимое этих файлов**.
Все остальные скрипты и конфигурации служат исключительно для проверки решений.

---

## 🧩 Пример решения

Файл `001_all_books.sql` может содержать, например:

```sql
-- Запрос: вывести все названия книг
SELECT title
FROM books;
```

Каждый файл должен содержать **один корректный SQL-запрос**.

---

## ⚙️ Локальный запуск и проверка

Перед началом убедитесь, что:

* Установлены **Docker** и **Docker Compose**.
* У вас **не запущена другая база данных PostgreSQL** или другие сервисы, занимающие **порт 5432**.
* Также убедитесь, что **порт 3000** свободен (на нём работает UI).

---

## 🖥️ Вариант 1. Проверка через веб-интерфейс (рекомендуется)

1. В корне проекта выполните:

   ```bash
   docker compose up -d
   ```

   Это поднимет:

    * PostgreSQL с тестовыми данными
    * Backend-проверку (`forgedl-checker`)
    * Веб-интерфейс **FORGEDL UI** (порт `3000`)

2. Откройте интерфейс проверки в браузере:
   👉 [http://localhost:3000](http://localhost:3000)

3. В UI вы можете:

    * Просматривать список тестов
    * Редактировать решения (SQL-запросы) прямо в браузере
    * Запускать тесты (все сразу, по отдельности или кейсами)
    * Смотреть результаты выполнения и проверки

4. После запуска или сохранения решение автоматически записывается в соответствующий файл в `student_solutions/`.

5. ⚠️ Если вы **изменили файл вручную** (в редакторе), чтобы увидеть изменения в интерфейсе — **переключитесь между
   тестами** (это обновит состояние в UI).

6. Для остановки всех сервисов:

   ```bash
   docker compose down
   ```

---

## 🧰 Вариант 2. Проверка через консоль (CLI-режим)

Если вы хотите запустить проверку без интерфейса, можно использовать CLI-режим:

> Предварительно необходимо поднять БД

```bash
docker run --rm \
  -v "$(pwd)/student_solutions:/app/student_solutions" \
  -e DB_HOST=host.docker.internal \
  -e DB_PORT=5432 \
  -e DB_USER=test \
  -e DB_PASSWORD=test \
  -e DB_NAME=testdb \
  minusd/forgedl-checker:1.0.0 --cli
```

Этот режим выполняет те же проверки, что и UI, но выводит результат прямо в консоль.

---

## 🌿 Работа с Git

1. Создайте ветку для домашнего задания:

   ```bash
   git checkout -b homework
   ```

2. Вносите изменения **только в файлы из папки `student_solutions/`**.

3. После завершения работы создайте **Merge Request** из ветки `homework` в `master` вашего форка.

⚠️ **Не отправляйте MR в основной репозиторий**, только в свой форк.

---

## 💡 Рекомендации

* Проверяйте решения локально перед отправкой.
* Следите за точностью SQL-запросов и соответствием ожидаемому результату.
* Если при запуске возникают конфликты по портам, остановите другие контейнеры или локальные сервисы (например,
  `postgres` или `frontend`, занимающий порт `3000`).